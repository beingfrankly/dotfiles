## Atuin Configuration

## where to store your database, default is your system data directory
db_path = "~/.local/share/atuin/history.db"

## where to store your encryption key, default is your system data directory
key_path = "~/.local/share/atuin/key"

## where to store your auth session token, default is your system data directory
session_path = "~/.local/share/atuin/session"

## date format used, either "us" or "uk"
dialect = "us"

## enable or disable automatic sync
auto_sync = false

## enable or disable automatic update checks
update_check = false

## address of the sync server
sync_address = "https://api.atuin.sh"

## how often to sync history. note that this is only triggered when a command
## is run, so sync intervals may well be longer
sync_frequency = "10m"

## which search mode to use
## possible values: prefix, fulltext, fuzzy, skim
search_mode = "fuzzy"

## which filter mode to use
## possible values: global, host, session, directory
filter_mode = "global"

## With workspace filtering enabled, Atuin will filter for commands executed
## in any directory within a git repository tree (default: false)
workspaces = false

## which filter mode to use when atuin is invoked from a shell up-key binding
## the accepted values are identical to those of "filter_mode"
filter_mode_shell_up_key_binding = "directory"

## which search mode to use when atuin is invoked from a shell up-key binding
## the accepted values are identical to those of "search_mode"
search_mode_shell_up_key_binding = "fuzzy"

## which style to use
## possible values: auto, full, compact
style = "auto"

## the maximum number of lines the interface should take up
## set it to 0 to always go full screen
inline_height = 20

## Invert the UI - put the search bar at the top
invert = false

## enable or disable showing a preview of the selected command
## useful when the command is longer than the terminal width and is cut off
show_preview = true

## what to do when the escape key is pressed when searching
## possible values: return-original, return-query
exit_mode = "return-original"

## possible values: emacs, subl
word_jump_mode = "emacs"

## characters that count as a part of a word
word_chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789_-"

## number of context lines to show when scrolling by pages
scroll_context_lines = 1

## use ctrl instead of alt as the shortcut modifier key for numerical UI shortcuts
## alt-0 .. alt-9
ctrl_n_shortcuts = false

## default history list format - can also be specified with the --format arg
history_format = "{time} · {duration}\t{command}"

## prevent commands matching any of these regexes from being written to history.
## Note that these regular expressions are unanchored, i.e. if they don't start
## with ^ or end with $, they'll match anywhere in the command.
history_filter = [
    "^secret-cmd",
    "^password",
    "^export .*SECRET.*=",
    "^export .*PASSWORD.*=",
    "^export .*TOKEN.*=",
]

## prevent commands run with cwd matching any of these regexes from being written
## to history. Note that these regular expressions are unanchored, i.e. if they don't
## start with ^ or end with $, they'll match anywhere in CWD.
cwd_filter = []

## Configure the maximum height of the preview to show.
## Useful when you have long scripts in your history that you want to distinguish
## by more than the first few lines.
max_preview_height = 4

## Configure whether or not to show the help row, which includes the current Atuin
## version (and whether an update is available), a keymap hint, and the total
## amount of commands in your history.
show_help = true

## Defaults to true. If enabled, upon hitting enter Atuin will immediately execute the command. Press tab to return to the shell and edit.
## Pressing ctrl-j will return to the shell without executing or editing the command.
enter_accept = true

## Defaults to "emacs".  This specifies the keymap on the startup of `atuin
## search`.  If this is set to "auto", the keymap mode in the Atuin search is
## automatically selected based on the shell's keymap where `atuin search` is
## invoked from.
keymap_mode = "emacs"

## Cursor style in each keymap mode.  If specified, the cursor style is changed
## in entering the cursor shape.  Available values are "default" and
## "{blink,steady}-{block,underline,bar}".
keymap_cursor = {}

## enable or disable showing tabs for search and inspect
show_tabs = true

## Configure whether or not to show the preview of the selected command by default.
## Ctrl+r toggles this at runtime
show_preview_by_default = true

## Configure the width of the command preview. If not specified, the preview
## width will be the terminal width minus the width of the search list.
preview_width_percent = 50

## Configure the width of the search list. If not specified, the search list
## width will be 40% of the terminal width.
search_list_width_percent = 40
